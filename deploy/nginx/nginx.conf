upstream passport {
    server passport:8080;
}

server {
    listen 80;
    server_name _;

    # Redirect all HTTP to HTTPS when TLS is enabled (uncomment below)
    # return 301 https://$host$request_uri;

    location / {
        proxy_pass         http://passport;
        proxy_http_version 1.1;

        proxy_set_header Host              $host;
        proxy_set_header X-Real-IP         $remote_addr;
        proxy_set_header X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        proxy_connect_timeout 60s;
        proxy_send_timeout    60s;
        proxy_read_timeout    60s;
    }
}

# Uncomment to enable HTTPS termination.
# Requires TLS certs mounted at /etc/nginx/certs/ and port 443 exposed.
#
# server {
#     listen 443 ssl;
#     server_name _;
#
#     ssl_certificate     /etc/nginx/certs/fullchain.pem;
#     ssl_certificate_key /etc/nginx/certs/privkey.pem;
#
#     ssl_protocols       TLSv1.2 TLSv1.3;
#     ssl_ciphers         HIGH:!aNULL:!MD5;
#
#     location / {
#         proxy_pass         http://passport;
#         proxy_http_version 1.1;
#
#         proxy_set_header Host              $host;
#         proxy_set_header X-Real-IP         $remote_addr;
#         proxy_set_header X-Forwarded-For   $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto https;
#
#         proxy_connect_timeout 60s;
#         proxy_send_timeout    60s;
#         proxy_read_timeout    60s;
#     }
# }
